overwrite: true
schema: 'src/schema/**/*.graphql'
generates:
  src/generated/graphql.tsx:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-resolvers'
  # ./graphql.schema.json:
  #     plugins:
  #         - 'introspection'
